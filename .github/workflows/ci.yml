name: CI

on:
  push:
    branches:
      - 'main'
      - 'feature/pipeline-deployment'


jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/maven.yml 
      - name: compile
        run: |
          echo "compiling"

  package:
    runs-on: ubuntu-latest
    needs: compile
    steps:
      - uses: ./.github/workflows/maven.yml 
      - name: compile
        run: |
          echo "packaging"
    