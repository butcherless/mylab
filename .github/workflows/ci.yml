name: CI

on:
  push:
    branches:
      - 'main'
      - 'feature/pipeline-deployment'
  pull_request:
    types: [ opened ]


jobs:
  init:
    runs-on: ubuntu-latest
    outputs: 
      o1: ${{ steps.env_setup.outputs.o1 }}
    steps:
      - id: env_setup
        name: Set Environment Variables
        run: |
          echo "grp=com.profile" >> $GITHUB_OUTPUT
          echo "o1=demo"
          echo "VER=1.0.0"
      - name: print local vars
        run: |
          echo "group: ${{ steps.env_setup.outputs.grp }} EOL"
          # echo "GRP2=${{ env.GRP }}" >> $GITHUB_OUTPUT

  build:
    runs-on: ubuntu-latest
    needs: init
    steps:
      - name: access var
        run: |
          echo "access var"
          echo "${{needs.init.outputs.o1}} EOL"